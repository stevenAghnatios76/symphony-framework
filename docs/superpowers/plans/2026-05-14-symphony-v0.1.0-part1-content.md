# Symphony v0.1.0 Specification Release — Part 1: Content

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close all Spec 7d gaps, add Codex adapter, and create all new content files for the v0.1.0 specification release.

**Architecture:** Symphony is a specification-only framework (no runtime engine yet) that runs inside AI coding platforms via adapters. This plan creates the remaining spec content (mobile agents, presets, vault, CLI registry, i18n, workspace isolation) plus a third adapter for Codex. All new files follow established patterns.

**Tech Stack:** YAML, XML, Markdown, JavaScript (ES modules, Vitest)

**Continues in:** `2026-05-14-symphony-v0.1.0-part2-tests-docs.md`

---

### Task 1: Fix package.json name

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Change package name**

In `package.json`, change:
```json
"name": "@stevenaghnatios/symphony-framework",
```
to:
```json
"name": "symphony-framework",
```

- [ ] **Step 2: Run structure test to verify**

Run: `npx vitest run tests/structure.test.js`
Expected: The "declares name as symphony-framework" test now passes.

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "fix: correct package.json name to symphony-framework"
```

---

### Task 2: Create mobile agents

**Files:**
- Create: `_symphony/dev/agents/designer-mobile.md`
- Create: `_symphony/dev/agents/mobile-tester.md`

- [ ] **Step 1: Create designer-mobile.md**

```markdown
---
id: designer-mobile
name: Mobile Designer
role: Mobile UI/UX Designer
model: opus
max_lines: 200
---

<agent id="designer-mobile" role="Mobile UI/UX Designer">
  <persona>
    <identity>Designs mobile interfaces that respect platform-native conventions. Bridges design systems to implementation through token-based handoffs, accessibility-first layouts, and motion that serves function over decoration.</identity>
    <expertise>
      - iOS Human Interface Guidelines and SF Symbols
      - Android Material Design 3 and dynamic color
      - Responsive mobile layouts (safe areas, notches, foldables)
      - Gesture design (swipe, long-press, pinch, drag)
      - Accessibility (VoiceOver, TalkBack, Dynamic Type, font scaling)
      - Design tokens and Figma-to-code handoff
      - Animation and motion design (meaningful transitions)
      - Dark mode and adaptive theming
      - Adaptive icons and launch screens
      - Platform navigation patterns (tab bar, bottom nav, drawer)
    </expertise>
    <operating-mode>Activated for mobile design stories and design-review workflows. Consumes PRDs, wireframes, and platform guidelines. Produces annotated design specs with token maps, accessibility notes, and platform-specific variants. Yields to Mobile Developer for implementation handoff.</operating-mode>
  </persona>

  <knowledge-sources>
    <trusted>
      <source>Story file (docs/implementation-artifacts/stories/)</source>
      <source>Architecture doc (docs/planning-artifacts/architecture.md)</source>
      <source>CLAUDE.md project rules</source>
      <source>Knowledge fragments: flutter/widget-patterns.md, typescript/react-patterns.md</source>
    </trusted>
    <verify>
      <source>Apple Human Interface Guidelines</source>
      <source>Material Design 3 documentation</source>
      <source>WCAG 2.1 mobile-specific success criteria</source>
    </verify>
    <untrusted>
      <source>Design trends from social media</source>
      <source>Auto-generated layout suggestions</source>
      <source>Screenshots without platform version context</source>
    </untrusted>
  </knowledge-sources>

  <skills-registry>
    <default>figma-integration, documentation-standards</default>
    <on-demand>edge-cases, validation-patterns</on-demand>
  </skills-registry>

  <disciplines>
    <self-critique threshold="0.85"/>
    <anti-rationalization>
      <excuse>We can use the same layout for both platforms</excuse>
      <rebuttal>iOS and Android users have different mental models. Respect platform conventions or users will fight the interface.</rebuttal>
      <excuse>Accessibility can be added later</excuse>
      <rebuttal>Retrofitting accessibility is 5x more expensive. Design for VoiceOver and TalkBack from day one.</rebuttal>
      <excuse>This animation looks cool</excuse>
      <rebuttal>Motion must serve function. Every animation needs a purpose: orient, guide, or confirm. Decorative motion is noise.</rebuttal>
    </anti-rationalization>
  </disciplines>

  <workflows-owned>
    <workflow>design-review</workflow>
  </workflows-owned>

  <memory-sidecar path="_symphony/_memory/designer-mobile-sidecar/"/>
</agent>
```

- [ ] **Step 2: Create mobile-tester.md**

```markdown
---
id: mobile-tester
name: Mobile Tester
role: Mobile QA Specialist
model: opus
max_lines: 200
---

<agent id="mobile-tester" role="Mobile QA Specialist">
  <persona>
    <identity>Validates mobile applications across devices, OS versions, and network conditions. Catches issues that simulators miss — thermal throttling, real GPS, haptic feedback, and carrier-specific network behavior. Ships confidence, not assumptions.</identity>
    <expertise>
      - Device matrix testing (phone, tablet, foldable)
      - OS version compatibility (minimum supported to latest)
      - Network condition simulation (offline, 2G, 3G, LTE, Wi-Fi transitions)
      - Gesture and touch testing (multi-touch, edge swipes, accessibility gestures)
      - Push notification validation (APNs, FCM, in-app, background)
      - App lifecycle testing (foreground, background, killed, low-memory)
      - Memory profiling and leak detection
      - Battery usage analysis
      - App store compliance (Apple Review Guidelines, Google Play policies)
      - Crash reporting integration (Crashlytics, Sentry)
    </expertise>
    <operating-mode>Activated for mobile-testing workflows. Inherits test strategy protocol from _base-test.md. Detects mobile framework from project markers. Runs tests via appropriate adapter (flutter-test-adapter, xctest-adapter). Produces device matrix reports with pass/fail per device-OS combination.</operating-mode>
  </persona>

  <base-dev ref="_symphony/testing/agents/_base-test.md"/>

  <knowledge-sources>
    <trusted>
      <source>Test plan (docs/planning-artifacts/test-plan.md)</source>
      <source>Architecture doc (docs/planning-artifacts/architecture.md)</source>
      <source>CLAUDE.md project rules</source>
      <source>Knowledge fragments: mobile/device-testing.md, mobile/gesture-testing.md, mobile/app-store-testing.md</source>
    </trusted>
    <verify>
      <source>Apple TestFlight documentation</source>
      <source>Firebase Test Lab documentation</source>
      <source>Device farm availability and pricing</source>
    </verify>
    <untrusted>
      <source>Emulator-only test results</source>
      <source>Single-device pass without matrix coverage</source>
      <source>Automated screenshot diffs without human review</source>
    </untrusted>
  </knowledge-sources>

  <skills-registry>
    <default>testing-patterns, edge-cases</default>
    <on-demand>security-basics, validation-patterns</on-demand>
  </skills-registry>

  <disciplines>
    <self-critique threshold="0.85"/>
    <anti-rationalization>
      <excuse>It works on the latest OS</excuse>
      <rebuttal>30% of users run older OS versions. Test the minimum supported version, not just the latest.</rebuttal>
      <excuse>Wi-Fi testing is sufficient</excuse>
      <rebuttal>Mobile networks are lossy and latent. Test on cellular, airplane mode transitions, and tunnel scenarios.</rebuttal>
      <excuse>The emulator passed all tests</excuse>
      <rebuttal>Emulators skip thermal throttling, real GPS, camera hardware, and haptic feedback. Ship after device testing.</rebuttal>
    </anti-rationalization>
  </disciplines>

  <workflows-owned>
    <workflow>mobile-testing</workflow>
  </workflows-owned>

  <memory-sidecar path="_symphony/_memory/mobile-tester-sidecar/"/>
</agent>
```

- [ ] **Step 3: Commit**

```bash
git add _symphony/dev/agents/designer-mobile.md _symphony/dev/agents/mobile-tester.md
git commit -m "feat(dev): add mobile designer and mobile tester agents (Spec 7d §2)"
```

---

### Task 3: Create environment presets

**Files:**
- Create: `_symphony/_config/presets/solo.yaml`
- Create: `_symphony/_config/presets/team.yaml`
- Create: `_symphony/_config/presets/enterprise.yaml`

- [ ] **Step 1: Create solo.yaml**

```yaml
id: solo
name: Solo Developer
description: Simplified gates and single-agent routing for individual developers

conductor:
  auto_dispatch_confidence: 0.70
  multi_agent_routing: false

gates:
  code_review_required: false
  security_review_required: false
  architecture_review_required: false

memory_hygiene:
  cadence: weekly
  auto_cleanup: true

hub:
  auto_refresh: false
  port: 3100

integrations:
  trello: false
  vault: false

ensemble:
  default_mode: self-review
  max_participants: 1

disciplines:
  self_critique_threshold: 0.80
  anti_rationalization: true
```

- [ ] **Step 2: Create team.yaml**

```yaml
id: team
name: Team
description: Full gate enforcement and multi-agent routing for small to mid-size teams

conductor:
  auto_dispatch_confidence: 0.80
  multi_agent_routing: true

gates:
  code_review_required: true
  security_review_required: false
  architecture_review_required: false

memory_hygiene:
  cadence: daily
  auto_cleanup: true

hub:
  auto_refresh: true
  port: 3100

integrations:
  trello: true
  vault: true

ensemble:
  default_mode: round-robin
  max_participants: 4

disciplines:
  self_critique_threshold: 0.85
  anti_rationalization: true
```

- [ ] **Step 3: Create enterprise.yaml**

```yaml
id: enterprise
name: Enterprise
description: Strict compliance, mandatory reviews, and audit trails for regulated environments

conductor:
  auto_dispatch_confidence: 0.90
  multi_agent_routing: true
  audit_trail: mandatory

gates:
  code_review_required: true
  security_review_required: true
  architecture_review_required: true
  stakeholder_review_on_prd: true

memory_hygiene:
  cadence: every-commit
  auto_cleanup: false
  retention_days: 90

hub:
  auto_refresh: true
  authentication: true
  port: 3100

integrations:
  trello: true
  vault: true

ensemble:
  default_mode: round-robin
  max_participants: 6

disciplines:
  self_critique_threshold: 0.90
  anti_rationalization: true
```

- [ ] **Step 4: Commit**

```bash
git add _symphony/_config/presets/
git commit -m "feat(config): add solo, team, enterprise environment presets (Spec 7d §3)"
```

---

### Task 4: Create workspace isolation protocol and knowledge fragment

**Files:**
- Create: `_symphony/core/protocols/workspace-isolation.xml`
- Create: `_symphony/dev/knowledge/patterns/workspace-isolation.md`

- [ ] **Step 1: Create workspace-isolation.xml**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<protocol id="workspace-isolation" version="1.0">
  <purpose>Ensure each agent operates within a bounded workspace, preventing cross-agent file collisions and enforcing explicit artifact handoffs.</purpose>

  <rules>
    <rule id="ws-1">Each agent activation receives a workspace root directory scoped to its current workflow step.</rule>
    <rule id="ws-2">Agents may read shared artifact directories (docs/planning-artifacts/, docs/implementation-artifacts/) but must not write to another agent's workspace.</rule>
    <rule id="ws-3">Cross-agent file access requires an explicit handoff entry in the workflow's output manifest.</rule>
    <rule id="ws-4">On workflow completion, the gate-enforcer verifies all workspace artifacts are accounted for — committed, handed off, or explicitly discarded.</rule>
    <rule id="ws-5">Orphaned workspaces older than the configured retention period are flagged by memory-hygiene protocol for cleanup.</rule>
  </rules>

  <workspace-layout>
    <directory purpose="agent-private">_symphony/_memory/{agent-id}-sidecar/</directory>
    <directory purpose="shared-read">docs/planning-artifacts/</directory>
    <directory purpose="shared-read">docs/implementation-artifacts/</directory>
    <directory purpose="handoff">docs/implementation-artifacts/handoffs/</directory>
    <directory purpose="checkpoints">_symphony/_memory/checkpoints/</directory>
  </workspace-layout>

  <handoff-protocol>
    <step>Producer agent writes artifact to its workspace.</step>
    <step>Producer records handoff in checkpoint: artifact path, target agent, sha256 hash.</step>
    <step>Consumer agent receives handoff reference via workflow-engine context.</step>
    <step>Consumer verifies hash before reading.</step>
  </handoff-protocol>

  <integration>
    <protocol ref="checkpoint-resume">Workspace state is included in checkpoint snapshots.</protocol>
    <protocol ref="memory-hygiene">Orphaned workspaces are cleaned on the configured cadence.</protocol>
    <protocol ref="gate-enforcer">Post-complete gate verifies workspace artifact accounting.</protocol>
  </integration>
</protocol>
```

- [ ] **Step 2: Create patterns/workspace-isolation.md knowledge fragment**

```markdown
# Workspace Isolation

**Principle:** Bound each agent's file access to a scoped workspace, enforce explicit handoffs for cross-agent artifacts, and clean up orphaned state on schedule.

## Pattern Examples

### Git Worktree Isolation
Spawn a fresh worktree per agent so file edits never collide:
```bash
git worktree add .worktrees/agent-dev-story feature-branch
```
Agent works in `.worktrees/agent-dev-story/`, merges back on completion.

### Container-Scoped Workspace
Bind-mount only the directories an agent needs:
```yaml
volumes:
  - ./src:/workspace/src:rw
  - ./docs/planning-artifacts:/workspace/docs:ro
```
Read-only mounts for shared artifacts prevent accidental writes.

### Handoff via Checkpoint
Producer writes to its private dir, records in checkpoint YAML:
```yaml
handoffs:
  - artifact: src/auth/login.ts
    target_agent: reviewer
    sha256: a1b2c3...
```
Consumer verifies hash before reading.

## Anti-Patterns

- Writing to shared directories without locks — causes race conditions when parallel agents run
- Skipping workspace cleanup — orphaned worktrees accumulate disk and confuse future runs
- Implicit file sharing — agent A assumes agent B wrote a file without checking the handoff manifest
- Using a single workspace for all agents — defeats isolation, merges become conflict-prone
- Hardcoding workspace paths — breaks when agents run in different environments

## Integration Points

- **checkpoint-resume protocol** — workspace state included in snapshots
- **memory-hygiene protocol** — orphan cleanup on configured cadence
- **gate-enforcer** — post-complete gate verifies artifact accounting
- **wave-executor** — parallel agents each get isolated workspaces
```

- [ ] **Step 3: Commit**

```bash
git add _symphony/core/protocols/workspace-isolation.xml _symphony/dev/knowledge/patterns/workspace-isolation.md
git commit -m "feat(core): add workspace isolation protocol and knowledge fragment (Spec 7d §4)"
```

---

### Task 5: Create vault schema files

**Files:**
- Create: `_symphony/core/vault/schema.yaml`
- Create: `_symphony/core/vault/codebase-index.yaml`
- Create: `_symphony/core/vault/query-patterns.md`

- [ ] **Step 1: Create schema.yaml**

```yaml
id: vault-schema
version: "1.0"
description: Knowledge graph schema for codebase intelligence

node_types:
  - id: file
    properties: [path, language, size_bytes, last_modified]
  - id: function
    properties: [name, file_path, line_start, line_end, signature]
  - id: class
    properties: [name, file_path, line_start, line_end]
  - id: module
    properties: [name, entry_path, exports]
  - id: dependency
    properties: [name, version, dev_only]
  - id: test
    properties: [name, file_path, covers]
  - id: config
    properties: [path, format, purpose]

edge_types:
  - id: imports
    from: [file, module]
    to: [file, module, dependency]
  - id: calls
    from: [function]
    to: [function]
  - id: tests
    from: [test]
    to: [function, class, module]
  - id: depends-on
    from: [module, dependency]
    to: [module, dependency]
  - id: owns
    from: [module]
    to: [file, function, class]
  - id: implements
    from: [class, function]
    to: [class]

metadata:
  last_indexed: "ISO 8601 timestamp"
  file_hash: "sha256"
  line_range: "[start, end]"

query_interface:
  - by-node-type
  - by-edge-type
  - by-path-glob
  - by-dependency-chain
```

- [ ] **Step 2: Create codebase-index.yaml**

```yaml
id: codebase-index
version: "1.0"
description: Rules for indexing the project codebase into the vault

file_patterns:
  typescript: ["*.ts", "*.tsx", "*.mts"]
  javascript: ["*.js", "*.jsx", "*.mjs"]
  python: ["*.py"]
  go: ["*.go"]
  dart: ["*.dart"]
  kotlin: ["*.kt", "*.kts"]
  swift: ["*.swift"]
  yaml: ["*.yaml", "*.yml"]
  xml: ["*.xml"]
  markdown: ["*.md"]

ignore_patterns:
  - ".git/**"
  - "node_modules/**"
  - "build/**"
  - "dist/**"
  - ".next/**"
  - "__pycache__/**"
  - ".dart_tool/**"
  - "*.min.js"
  - "*.lock"

indexing_frequency:
  on_change: true
  on_commit: true
  scheduled: "daily"

storage:
  location: "_symphony/_memory/vault-index/"
  format: "yaml"
  max_index_size_mb: 50
```

- [ ] **Step 3: Create query-patterns.md**

```markdown
# Vault Query Patterns

**Principle:** Query the codebase knowledge graph for structural relationships — dependencies, coverage, and change impact — rather than grepping text.

## Pattern Examples

### Dependency Impact Query
Find everything that depends on a module before changing it:
```yaml
query: by-dependency-chain
root: "src/auth/session.ts"
direction: dependents
depth: 3
```
Returns: all files, functions, and tests that transitively depend on session.ts.

### Coverage Gap Query
Find functions with no test coverage:
```yaml
query: by-node-type
type: function
filter: "NOT EXISTS edge(tests, *, this)"
```
Returns: functions that have no inbound `tests` edge from any test node.

### Change Impact Query
Given a set of changed files, find affected tests:
```yaml
query: by-edge-type
edge: tests
direction: inbound
targets: ["src/api/routes.ts", "src/api/middleware.ts"]
```
Returns: all test nodes that cover the changed files.

## Anti-Patterns

- Over-indexing generated files — index source, not build output
- Querying a stale index — always check `last_indexed` before trusting results
- Using vault instead of reading the file — vault gives structure, not content; read the file for implementation details
- Indexing without ignore patterns — node_modules alone can produce millions of nodes
- Treating vault as the only truth — vault supplements, never replaces, reading code

## Integration Points

- **explore-codebase workflow** — uses vault queries to map architecture
- **test-architect** — uses coverage gap queries to prioritize test writing
- **developer agent** — uses dependency impact queries before refactoring
- **memory-hygiene protocol** — vault index is cleaned on the configured cadence
```

- [ ] **Step 4: Commit**

```bash
git add _symphony/core/vault/
git commit -m "feat(core): add vault knowledge graph schema, indexing rules, and query patterns (Spec 7d §5)"
```

---

### Task 6: Create CLI command registry and SDK interface

**Files:**
- Create: `_symphony/core/cli/command-registry.yaml`
- Create: `_symphony/core/cli/sdk-interface.yaml`

Note: These are adapter metadata files — they define the command surface that adapters translate into platform-native commands. Symphony has no standalone CLI.

- [ ] **Step 1: Create command-registry.yaml**

```yaml
id: command-registry
version: "1.0"
description: Maps Symphony commands to workflows. Adapters consume this registry to generate platform-native commands.

categories:
  - id: lifecycle
    description: Full product lifecycle workflows
  - id: dev
    description: Developer tools and utilities
  - id: creative
    description: Creative and ideation workflows
  - id: testing
    description: Testing and quality assurance
  - id: utility
    description: Cross-cutting utilities
  - id: admin
    description: Framework administration

commands:
  # Phase 1 — Analysis
  - id: brainstorm
    command: symphony-brainstorm
    description: Multi-agent brainstorming roundtable
    workflow_ref: lifecycle/workflows/1-analysis/brainstorm
    category: lifecycle
  - id: market-research
    command: symphony-market-research
    description: Market research and competitive analysis
    workflow_ref: lifecycle/workflows/1-analysis/market-research
    category: lifecycle
  - id: domain-research
    command: symphony-domain-research
    description: Deep domain research
    workflow_ref: lifecycle/workflows/1-analysis/domain-research
    category: lifecycle
  - id: tech-research
    command: symphony-tech-research
    description: Technology stack research
    workflow_ref: lifecycle/workflows/1-analysis/tech-research
    category: lifecycle
  - id: product-brief
    command: symphony-product-brief
    description: Generate product brief from research
    workflow_ref: lifecycle/workflows/1-analysis/product-brief
    category: lifecycle
  - id: advanced-elicitation
    command: symphony-advanced-elicitation
    description: Advanced requirements elicitation
    workflow_ref: lifecycle/workflows/1-analysis/advanced-elicitation
    category: lifecycle

  # Phase 2 — Planning
  - id: create-prd
    command: symphony-create-prd
    description: Create product requirements document
    workflow_ref: lifecycle/workflows/2-planning/create-prd
    category: lifecycle
  - id: validate-prd
    command: symphony-validate-prd
    description: Validate PRD completeness
    workflow_ref: lifecycle/workflows/2-planning/validate-prd
    category: lifecycle
  - id: create-ux
    command: symphony-create-ux
    description: Create UX specifications
    workflow_ref: lifecycle/workflows/2-planning/create-ux
    category: lifecycle

  # Phase 3 — Solutioning
  - id: create-arch
    command: symphony-create-arch
    description: Create architecture document
    workflow_ref: lifecycle/workflows/3-solutioning/create-arch
    category: lifecycle
  - id: create-epics
    command: symphony-create-epics
    description: Break architecture into epics
    workflow_ref: lifecycle/workflows/3-solutioning/create-epics
    category: lifecycle
  - id: test-design
    command: symphony-test-design
    description: Design test strategy
    workflow_ref: lifecycle/workflows/3-solutioning/test-design
    category: lifecycle
  - id: threat-model
    command: symphony-threat-model
    description: Security threat modeling
    workflow_ref: lifecycle/workflows/3-solutioning/threat-model
    category: lifecycle

  # Phase 4 — Implementation
  - id: sprint-plan
    command: symphony-sprint-plan
    description: Plan sprint from epics
    workflow_ref: lifecycle/workflows/4-implementation/sprint-plan
    category: lifecycle
  - id: dev-story
    command: symphony-dev-story
    description: Implement a user story
    workflow_ref: lifecycle/workflows/4-implementation/dev-story
    category: lifecycle
  - id: code-review
    command: symphony-code-review
    description: Review code changes
    workflow_ref: lifecycle/workflows/4-implementation/code-review
    category: lifecycle

  # Phase 5 — Deployment
  - id: release-plan
    command: symphony-release-plan
    description: Create release plan
    workflow_ref: lifecycle/workflows/5-deployment/release-plan
    category: lifecycle
  - id: deploy-checklist
    command: symphony-deploy-checklist
    description: Pre-deployment checklist
    workflow_ref: lifecycle/workflows/5-deployment/deploy-checklist
    category: lifecycle

  # Anytime
  - id: quick-spec
    command: symphony-quick-spec
    description: Fast-track specification for simple features
    workflow_ref: lifecycle/workflows/anytime/quick-spec
    category: dev
  - id: quick-dev
    command: symphony-quick-dev
    description: Fast-track implementation for simple features
    workflow_ref: lifecycle/workflows/anytime/quick-dev
    category: dev
  - id: spike
    command: symphony-spike
    description: Time-boxed technical investigation
    workflow_ref: lifecycle/workflows/anytime/spike
    category: dev
  - id: explore-codebase
    command: symphony-explore-codebase
    description: Map and understand codebase architecture
    workflow_ref: lifecycle/workflows/anytime/explore-codebase
    category: dev
  - id: adversarial-review
    command: symphony-adversarial-review
    description: Stress-test decisions with adversarial critique
    workflow_ref: lifecycle/workflows/anytime/adversarial-review
    category: utility
  - id: hub
    command: symphony-hub
    description: Launch the Symphony dashboard
    workflow_ref: lifecycle/workflows/anytime/hub
    category: admin
  - id: status
    command: symphony-status
    description: Show current workflow and project status
    workflow_ref: lifecycle/workflows/anytime/status
    category: admin
  - id: i18n-setup
    command: symphony-i18n-setup
    description: Set up internationalization for a project
    workflow_ref: lifecycle/workflows/anytime/i18n-setup
    category: dev
```

- [ ] **Step 2: Create sdk-interface.yaml**

```yaml
id: sdk-interface
version: "1.0"
description: Programmatic API surface for Symphony. Adapters implement this interface to expose Symphony capabilities to their host platform.

methods:
  - id: start-workflow
    description: Start a named workflow with optional inputs
    params: [workflow_id, inputs, options]
    returns: workflow_run_id
  - id: get-status
    description: Get current status of a workflow run or the framework
    params: [workflow_run_id]
    returns: status_snapshot
  - id: list-agents
    description: List all available agents with their capabilities
    params: [filter]
    returns: agent_list
  - id: dispatch-agent
    description: Activate an agent for a specific task
    params: [agent_id, task, context]
    returns: agent_run_id
  - id: query-vault
    description: Run a vault query against the codebase knowledge graph
    params: [query_type, query_params]
    returns: query_results
  - id: get-checkpoint
    description: Retrieve a checkpoint snapshot
    params: [checkpoint_id]
    returns: checkpoint_data

events:
  - id: on-workflow-start
    payload: [workflow_id, workflow_run_id, timestamp]
  - id: on-step-complete
    payload: [workflow_run_id, step_id, status, artifacts]
  - id: on-gate-check
    payload: [workflow_run_id, gate_id, passed, details]
  - id: on-workflow-end
    payload: [workflow_run_id, status, duration, artifacts]

extensions:
  - id: custom-agent-registration
    description: Register a user-defined agent with Symphony
    params: [agent_spec_path]
  - id: custom-workflow-registration
    description: Register a user-defined workflow with Symphony
    params: [workflow_dir_path]
  - id: plugin-loader
    description: Load a plugin that extends Symphony capabilities
    params: [plugin_manifest_path]
```

- [ ] **Step 3: Commit**

```bash
git add _symphony/core/cli/
git commit -m "feat(core): add command registry and SDK interface for adapter consumption (Spec 7d §6)"
```

---

### Task 7: Create i18n knowledge fragment and workflow

**Files:**
- Create: `_symphony/dev/knowledge/patterns/internationalization.md`
- Create: `_symphony/lifecycle/workflows/anytime/i18n-setup/workflow.yaml`
- Create: `_symphony/lifecycle/workflows/anytime/i18n-setup/instructions.xml`
- Create: `_symphony/lifecycle/workflows/anytime/i18n-setup/template.md`
- Create: `_symphony/lifecycle/workflows/anytime/i18n-setup/checklist.md`

- [ ] **Step 1: Create internationalization.md knowledge fragment**

```markdown
# Internationalization (i18n)

**Principle:** Extract all user-facing strings into locale resource bundles from the start, use ICU message format for pluralization and gender, and validate locale completeness in CI.

## Pattern Examples

### ICU Message Format
Handle plurals and gender without string concatenation:
```
{count, plural,
  =0 {No items}
  one {# item}
  other {# items}
}
```
Works across libraries: react-intl, flutter_localizations, go-i18n.

### React with react-intl
```tsx
import { FormattedMessage } from 'react-intl';
<FormattedMessage id="cart.items" defaultMessage="{count, plural, one {# item} other {# items}}" values={{ count }} />
```

### Flutter Localization
```dart
// l10n/app_en.arb
{ "cartItems": "{count, plural, =0{No items} one{1 item} other{{count} items}}" }
// Usage: AppLocalizations.of(context)!.cartItems(count)
```

### RTL Layout Support
Use logical properties instead of physical directions:
```css
/* Instead of margin-left, use: */
margin-inline-start: 16px;
/* Instead of text-align: left, use: */
text-align: start;
```

## Anti-Patterns

- Concatenating translated strings — word order differs across languages; use parameterized messages
- Hardcoding locale to en-US — detect from user preferences, fall back gracefully
- Translating after development — extraction is 10x harder retroactively
- Assuming LTR layout — Arabic, Hebrew, and Urdu are RTL; test mirrored layouts
- Using string length for UI sizing — German text averages 30% longer than English

## Integration Points

- **react-intl** — React i18n with ICU message format
- **flutter_localizations** — Flutter's built-in ARB-based localization
- **go-i18n** — Go struct-based message catalogs
- **FormatJS CLI** — Extract and compile ICU messages for JavaScript
- **CI locale validation** — Fail build if any locale file has missing keys
```

- [ ] **Step 2: Create i18n-setup workflow.yaml**

```yaml
id: i18n-setup
owner: developer
model: sonnet
phase: anytime
description: Set up internationalization infrastructure for a project — audit strings, select library, configure locales, extract resources

execution:
  mode: sequential
  wave_eligible: false

inputs:
  required: []
  optional:
    - project_path
    - target_locales

outputs:
  primary: docs/planning-artifacts/i18n-setup-report.md
  traceable_to: []

gates:
  pre_start: []
  post_complete:
    - At least one locale resource file exists
    - CI validation step documented

disciplines:
  self_critique: true
  anti_rationalization: true
  trust_levels: true
```

- [ ] **Step 3: Create i18n-setup instructions.xml**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<instructions workflow="i18n-setup">
  <step id="1" name="audit">
    <action>Scan the codebase for hardcoded user-facing strings. Check UI components, error messages, validation messages, email templates, and CLI output. Record the count and locations.</action>
    <output>String audit table: file path, line, string, context.</output>
  </step>

  <step id="2" name="select-library">
    <action>Based on the project stack (detected from package.json, pubspec.yaml, go.mod, etc.), recommend an i18n library. Load knowledge fragment: patterns/internationalization.md.</action>
    <output>Library recommendation with rationale.</output>
  </step>

  <step id="3" name="configure-locales">
    <action>Create locale resource directory structure and base locale files. Set up the default locale (en) and any target locales specified in inputs.</action>
    <output>Directory structure and base resource files created.</output>
  </step>

  <step id="4" name="extract-strings">
    <action>Extract audited strings into the locale resource format chosen in step 2. Use ICU message format for any strings with plurals, numbers, or dates.</action>
    <output>Populated resource bundles for the default locale.</output>
  </step>

  <step id="5" name="ci-validation">
    <action>Document a CI step that validates locale file completeness — every key in the default locale must exist in all target locales. Missing keys fail the build.</action>
    <output>CI validation configuration documented in the report.</output>
  </step>

  <apply-template path="template.md"/>
</instructions>
```

- [ ] **Step 4: Create i18n-setup template.md**

```markdown
# i18n Setup Report

**Date:** {{date}}
**Project:** {{project_path}}
**Target Locales:** {{target_locales}}

## String Audit

| File | Line | String | Context |
|------|------|--------|---------|
{{audit_table}}

**Total strings found:** {{string_count}}

## Library Selection

**Chosen library:** {{library}}
**Rationale:** {{rationale}}

## Locale Configuration

**Directory structure:**
{{directory_structure}}

**Base locale:** {{default_locale}}
**Target locales:** {{target_locales}}

## Extraction Summary

**Strings extracted:** {{extracted_count}}
**ICU formatted:** {{icu_count}}

## CI Validation

{{ci_config}}

## Next Steps

{{next_steps}}
```

- [ ] **Step 5: Create i18n-setup checklist.md**

```markdown
# i18n Setup — Completion Checklist

## Pre-Start
- [ ] Project stack detected (package.json, pubspec.yaml, go.mod, etc.)

## Post-Complete
- [ ] String audit completed with file locations
- [ ] i18n library selected with rationale
- [ ] Locale resource directory created
- [ ] Base locale file populated with extracted strings
- [ ] ICU message format used for plurals/numbers/dates
- [ ] CI validation step documented
- [ ] Report written to docs/planning-artifacts/i18n-setup-report.md
```

- [ ] **Step 6: Commit**

```bash
git add _symphony/dev/knowledge/patterns/internationalization.md _symphony/lifecycle/workflows/anytime/i18n-setup/
git commit -m "feat: add i18n knowledge fragment and setup workflow (Spec 7d §7)"
```

---

### Task 8: Create Codex adapter

**Files:**
- Create: `adapters/codex/adapter.yaml`
- Create: `adapters/codex/translator.js`
- Create: `adapters/codex/templates/agents.md.tmpl`
- Create: `_symphony/core/adapter-registry/codex.yaml`

- [ ] **Step 1: Create adapter.yaml**

```yaml
id: codex
target_tool: "OpenAI Codex CLI"
translator_entry: "./translator.js"
templates_dir: "./templates"

output_paths:
  commands: ".codex/"
  config: "AGENTS.md"

command_file_extension: ".md"
command_naming_pattern: "symphony-{action}"
```

- [ ] **Step 2: Create translator.js**

```javascript
// Symphony Codex adapter — translator
// See architecture spec §7.3

import { readFileSync, writeFileSync, mkdirSync } from 'node:fs';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';
import { enumerateWorkflows, renderTemplate } from '../../lib/adapter-utils.js';

const __filename = fileURLToPath(import.meta.url);
const adapterDir = dirname(__filename);

export async function translate(corePath, userProjectPath, options = {}) {
  const workflows = enumerateWorkflows(corePath);
  const templatePath = join(adapterDir, 'templates', 'agents.md.tmpl');
  const template = readFileSync(templatePath, 'utf8');
  const outDir = join(userProjectPath, '.codex');

  mkdirSync(outDir, { recursive: true });

  let count = 0;
  for (const wf of workflows) {
    const rendered = renderTemplate(template, {
      description: wf.description,
      workflow_path: wf.workflowPath,
    });
    writeFileSync(join(outDir, `symphony-${wf.id}.md`), rendered);
    count++;
  }

  const entryContent = [
    '# Symphony Agent Instructions',
    '',
    'Load `_symphony/core/engine/conductor.xml` first.',
    'Parse the user\'s goal and route to the appropriate workflow.',
    '',
    'Available workflows are listed in `.codex/` as `symphony-*.md` files.',
    '',
  ].join('\n');
  writeFileSync(join(userProjectPath, 'AGENTS.md'), entryContent);

  return { commands_generated: count, entry_command: 'AGENTS.md' };
}

export const metadata = {
  id: 'codex',
  stub: false,
  specReference: 'docs/superpowers/specs/2026-04-10-symphony-adapters-design.md',
};
```

- [ ] **Step 3: Create agents.md.tmpl**

```markdown
# {{description}}

Load `_symphony/core/engine/conductor.xml` first.
Then process workflow: `{{workflow_path}}`.
Execute in normal mode unless the user has switched to YOLO.
```

- [ ] **Step 4: Create codex.yaml adapter registry entry**

```yaml
id: codex
target_tool: "OpenAI Codex CLI"
target_tool_url: "https://github.com/openai/codex"

output_paths:
  commands: ".codex/"
  config: "AGENTS.md"

command_file_extension: ".md"
command_naming_pattern: "symphony-{action}"

supports:
  model_frontmatter: false
  slash_commands: false
  agent_memory: false
  model_selection_per_command: false

capabilities_declared:
  - sequential_workflows
  - gate_enforcement
  - self_critique
```

- [ ] **Step 5: Commit**

```bash
git add adapters/codex/ _symphony/core/adapter-registry/codex.yaml
git commit -m "feat(adapters): add Codex adapter with registry entry"
```
